require 'spec_helper'

describe RepoHelper do
  describe "generate custom urls for navigating source" do
    #context "if there is a longer path at the end of url" do
      #helper.generate_repo_url("u","r","o","b",nil,"f").should equal("/u/r/o/b/f")
    #end
    #context "if there is not a longer path at the end of url" do
      #helper.generate_repo_url("u","r","o","b","p","f").should equal("/u/r/o/b/p/f")
    #end
  end
end
